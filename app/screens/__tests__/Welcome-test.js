import 'react-native'
import React from 'react'
import Welcome from '../Welcome'

describe('Welcome screen', () => {
  // BUG: with react-native-navigator
  // it('renders correctly', () => {
  //   const tree = renderer.create(
  //     <Welcome />
  //   ).toJSON();
  //   expect(tree).toMatchSnapshot();
  // })
})